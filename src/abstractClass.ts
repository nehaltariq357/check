// abstract class means like , we have interface , we use interface for define only time time property and use it in  multiples object , same like but we use abstract instead of interface , means logic is same is define one time use multiple time